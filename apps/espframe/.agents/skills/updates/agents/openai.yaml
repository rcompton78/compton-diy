interface:
  display_name: "Updates"
  short_description: "Summarize recent user-facing project changes"
  default_prompt: "Use $updates to summarize the user-facing changes from the last 7 days."

policy:
  allow_implicit_invocation: true
